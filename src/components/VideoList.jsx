import { react } from 'react';
import { Video } from 'components';

const VideoList = () => (
    <div>
        <div>
            Add Btn
        </div>
        <div>
            <Video />
        </div>
    </div>
);

export default VideoList;
